[project]
name = "dlt-alpaca2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dlt[postgres]>=1.10.0",
    "psutil>=7.0.0",
]
